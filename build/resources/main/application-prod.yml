spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: none
  datasource:
    url:
    username: root
    password:
  sql:
    init:
      mode: always
server:
  port: 9090