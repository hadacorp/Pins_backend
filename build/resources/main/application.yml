spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/pins?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password:
  sql:
    init:
      mode: always
server:
  port: 8080